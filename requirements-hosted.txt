# Lightweight runtime deps for the free live demo (e.g. Render, 512 MB RAM).
# Excludes the document-RAG stack (faiss / sentence-transformers / torch / pypdf)
# so the app fits a small free instance. Run with DISABLE_DOC_TOOL=1.
# The full stack (incl. RAG) is in requirements.txt for local + CI.

# --- Core agent / LLM ---
langgraph==0.2.39
langchain-core==0.3.15
groq==0.11.0
google-generativeai==0.8.3
anthropic==0.39.0
openai==1.54.3

# --- Tools (no torch) ---
yfinance==0.2.44
duckduckgo-search==6.3.4

# --- Data / numeric ---
pandas==2.2.3
numpy==2.1.3

# --- Backend / API ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
slowapi==0.1.9

# --- Frontend ---
streamlit==1.39.0
requests==2.32.3

# --- Observability ---
structlog==24.4.0
